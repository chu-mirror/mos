@
\section{Drivers}
Copied verbatim from other projects.

@
\subsection{UART}

<<uart's port>>=
void uartinit(void);
void uartintr(void);
void uartputc(int);
void uartputc_sync(int);
int uartgetc(void);

@ %def uartinit uartintr uartputc uartputc_sync uartgetc

<<repertoire of subsystem layer>>=
<<uart's port>>
